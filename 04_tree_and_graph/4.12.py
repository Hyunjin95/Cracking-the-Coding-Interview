import unittest
from tree import BinarySearchTree



# There is a binary tree where the value of each node is an integer (positive or negative).
# We want to count the number of routes that the sum of integers is a specific number.
# Routes do not have to start from the root and end at the leaf nodes, however, it should go downwards.
# That is, you can only move from the parent nodes to the child nodes.
def find_route():
    pass


class Test(unittest.TestCase):
    def test(self):
        pass


if __name__ == '__main__':
    unittest.main()
